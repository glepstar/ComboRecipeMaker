# コンボレシピ作成支援ツール

## 目的

- 格闘ゲームのコンボレシピを、ボタン操作で短時間に組み立てられるツールを提供する
- ルールや記法の複雑さを抑えて、コンボのプレビューに集中できるようにする
- データ管理とコンボ作成を切り分け、メイン機能を最前面に出す

## 現在の設計方針

### 1. メイン機能はコンボビルダー
- コンボビルダーを最優先の画面として配置する
- 通常技、ジャンプ、その他、必殺技をボタンとして表示する
- ボタンを順番に押すことで、コンボ文字列を組み立てる
- 追加された要素は最新順に積み重なり、1つ戻す・クリアで巻き戻せる
- 要素の並び替えや個別削除は行わない
- プレビュー部分はボタンの上に表示し、結果テキストを主役にする

### 2. 技登録はタブ化して補助機能にする
- 「コンボビルダー」「技登録」「JSON」の3タブで管理する
- 通常技・ジャンプ・その他はゲーム単位で登録する
- 必殺技はキャラクター単位で登録する
- キャラクターが未作成でも通常技は登録可能にする
- 必殺技の登録はキャラクターを選択している場合のみ有効にする

### 3. データ管理は補助機能として扱う
- JSONのインポート/エクスポートは専用タブに配置する
- 保存済みコンボはキャラクターごとに管理する
- 画面上では、コンボの入力とプレビューを優先し、JSON編集は後段に置く

## データ構造

- ゲーム
  - 通常技
  - ジャンプ
  - その他
  - キャラクター
    - 必殺技
    - コンボレシピ

## コンボのルール

- 各行動はボタンとして登録できる
- 1回押すごとに、最新の要素が追加される
- 直前の入力だけを取り消せる
- 最後までクリアできる
- 各要素の間は連結して表示する
- プレビューだけを見て、保存・編集を行う

## 変更済みのUX方針

- ドラッグアンドドロップによる要素並び替えは不要
- 各要素の個別削除は不要
- 連続タップで構築するシンプルな入力方式を採用
- 登録履歴表示は行わず、テキストプレビューのみを表示
- 必殺技の表記切替は行わず、登録名そのものを使う
- 画面は「コンボ作成が主役」のレイアウトに整理

## 技術実装方針

- フロントエンドは React + Vite で実装する
- SPA（Single Page Application）として動作する
- ローカルストレージを使ってデータを永続化する
- PWA としてインストール可能にする
- GitHub Pages で静的配布できる構成を前提にする

## 機能一覧

- ゲームの追加・削除
- キャラクターの追加・削除
- 通常技の追加・削除
- ジャンプ技の追加・削除
- その他技の追加・削除
- 必殺技の追加・削除
- コンボの組み立て
- 1つ戻す
- クリア
- コンボの保存
- 保存済みコンボの編集・削除
- JSONの出力・読込

## 受け入れ条件

- コンボビルダーが最も目立つ画面になっている
- 通常技の登録がキャラクター未作成でも可能である
- 必殺技の登録はキャラクター選択時に行う
- 技登録やJSONはタブで切り替えられる
- コンボの入力はボタン操作中心で扱いやすい
- 最低限のローカル保存と配布性を確保する
- UIは直感的で簡潔に保つ
